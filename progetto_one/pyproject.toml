[tool.poetry]
name = "progetto-one"
version = "0.1.0"
description = ""
authors = ["EVIOLAL9U <leonardo.viola.nttdata@skytv.it>"]
readme = "README.md"

[tool.poetry.dependencies]
python = "^3.8"


[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
